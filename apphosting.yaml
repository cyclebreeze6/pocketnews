# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  maxInstances: 1
env:
  - variable: NODE_ENV
    value: production
  - variable: CRON_SECRET
    # IMPORTANT: Change this value in the Firebase Console Settings -> Secrets or Environment Variables if you want to override this.
    # When triggering the sync externally, include this header: Authorization: Bearer pNews_sync_7k2X9m5q8vR1tB4z
    value: "pNews_sync_7k2X9m5q8vR1tB4z"

cron:
  - schedule: "every 20 minutes"
    uri: "/api/cron/sync-breaking-news"
    timeoutSeconds: 540
  - schedule: "every 1 hours"
    uri: "/api/cron/sync-all-channels"
    timeoutSeconds: 540
